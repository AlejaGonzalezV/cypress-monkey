{
  "name": "monkey",
  "version": "1.0.0",
  "description": "Random and smart monkey testing tool for web apps",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "monkey": ".\\node_modules\\.bin\\cypress open --config-file .\\monkey-config.json"
  },
  "dependencies": {
    "cypress": "^4.2.0",
    "faker": "^4.1.0"
  }
}
